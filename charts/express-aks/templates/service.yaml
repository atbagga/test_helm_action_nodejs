apiVersion: v1
kind: Service
metadata:
  name: {{ include "myaks.fullname" . }}
  labels:
    {{- include "myaks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "myaks.selectorLabels" . | nindent 4 }}
